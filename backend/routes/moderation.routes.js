/** routes/moderation.routes.js — stub */
const express = require('express');
const router  = express.Router();
router.get('/', (req, res) => res.status(501).json({ message: 'Phase 2: Moderation routes — coming soon' }));
module.exports = router;
